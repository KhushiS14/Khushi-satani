#include<stdio.h>
#include<conio.h>
main()
{
	int a , b ,c , d ;
	clrscr();

	printf("value of a is :");
	scanf("%d",&a);
	printf("value of b is :");
	scanf("%d",&b);
	printf("value of c is :");
	scanf("%d",&c);
	printf("value of d is :");
	scanf("%d",&d);

	if(a==b && b==c && c==d && d==a)
	  {
	     printf("all are same...");
	  }
       else
	  {
	     if (a==b)
		{
		   printf("a and b are same...");
		}
	     else if (b==c)
		{
		   printf("b and c are same...");
		}
	     else if (c==d)
		{
		   printf("c and d are same ...");
		}
	     else if (d==a)
		{
		   printf("d ane a are same...");
		}
      else
	 {
	   if (a>b)
	      {
	      if (a>c)
		{
		   if (c<d)
		      {
			  printf("c is minimum");
		      }
		   else
		      {
			   printf ("d is minimum");
		      }
		    if (c<d)
		       {
			    printf("c is minimum");
		       }
		    else
		       {
			    printf("d is minimum");
		       }
		   }
	       }
      else
	  {
	     if(b<d)
		{
		     printf("b is minimum");
		}
	     else
		{
		     printf("d is minimum");
		}
	     }
	  }
	  }
    getch();
    }
